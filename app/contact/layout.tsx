export const metadata = {
  title: "Contact | Sunrise Estates",
  description:
    "Get in touch with Sunrise Estates about a coastal property asset.",
};

export default function ContactLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
